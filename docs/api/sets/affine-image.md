Use `convax.operations.affine_map` for dispatch; construct `AffineImage` directly only when requiring its lazy support-function form instead of retaining an exact set type.

::: convax.sets.AffineImage
