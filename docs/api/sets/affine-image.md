# AffineImage

Use `convax.affine_map` for normal dispatch. Construct `AffineImage` directly to
require a lazy support-function representation when another exact representation
is available.

::: convax.AffineImage
