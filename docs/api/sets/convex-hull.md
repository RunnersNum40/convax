Use `convax.operations.convex_hull` for dispatch; construct `ConvexHull` directly only when requiring its lazy support-function form instead of retaining an exact convex-hull-closed set type.

::: convax.sets.ConvexHull
