Use `convax.operations.convex_hull` for dispatch; construct `ConvexHull` directly only when requiring its lazy support-function representation instead of retaining an exact convex-hull-closed representation.

::: convax.sets.ConvexHull
