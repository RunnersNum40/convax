# ConvexHull

Use `convax.convex_hull` for normal dispatch. Construct `ConvexHull` directly to
require its lazy support-function representation instead of retaining a matching
convex-hull-closed representation.

::: convax.ConvexHull
