Use `convax.convex_hull` for normal dispatch; construct `ConvexHull` directly only to require its lazy support-function representation instead of retaining a matching convex-hull-closed representation.

::: convax.ConvexHull
