Use `convax.operations.minkowski_sum` for normal dispatch; construct `MinkowskiSum` directly only to require its lazy support-function form instead of retaining a matching Minkowski-sum-closed set type.

::: convax.sets.MinkowskiSum
